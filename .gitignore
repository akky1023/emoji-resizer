# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output directory from tests or runs
/output/

# Dependency directories (only needed if you keep vendor)
# vendor/
